import { createChatBotMessage } from 'react-chatbot-kit';
const config = {
  botName : 'LegalAssitant',
  initialMessages: [createChatBotMessage(`Welcome To LegalAssistant`)],

};

export default config;